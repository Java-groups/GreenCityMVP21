package greencity.enums;

public enum FriendshipRequestStatus {
    PENDING,     // Request sent and waiting for a response
    ACCEPTED,    // Request is accepted
    REJECTED,    // Request was rejected by recipient
    CANCELLED    // Request was canceled by sender
}
