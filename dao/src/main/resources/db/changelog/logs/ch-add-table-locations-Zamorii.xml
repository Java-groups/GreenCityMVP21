<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.15.xsd">

    <changeSet id="create-table-locations" author="Serhii Zamorii">
        <createTable tableName="locations">
        <column name="id" type="BIGINT" autoIncrement="true">
            <constraints primaryKey="true" nullable="false"/>
        </column>
        <column name="address" type="VARCHAR(255)">
            <constraints nullable="false"/>
        </column>
        <column name="lat" type="FLOAT8">
            <constraints nullable="false"/>
        </column>
        <column name="lng" type="FLOAT8">
            <constraints nullable="false"/>
        </column>
    </createTable>

        <sql>
            ALTER TABLE locations
                ADD CONSTRAINT locations_lat_check CHECK (lat &gt;= -90 AND lat &lt;= 90);
        </sql>

        <sql>
            ALTER TABLE locations
                ADD CONSTRAINT locations_lng_check CHECK (lng &gt;= -180 AND lng &lt;= 180);
        </sql>
    </changeSet>

</databaseChangeLog>
